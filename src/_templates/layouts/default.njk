<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <meta name="theme-color" content="#000">

    <title>{{ title }}</title>

    <link rel="stylesheet" href="{{ stylesPath }}/styles.css">
</head>

<body>
    <header class="header">
        <div class="brand header__brand">
            <a class="brand__link" href="/">
                <object type="image/svg+xml" data="{{imagesPath}}/logo.svg">
                    <img aria-hidden="true" src="{{imagesPath}}/logo.png">
                </object>
            </a>
        </div>
        {% import 'macros/nav.njk' as nav %}
        {{ nav.active(slug, menu) }}
    </header>

    {% block content %}
    {% endblock %}

{% include "partials/footer.njk" %}

</body>
</html>
